import 'package:flutter/material.dart';

class QuestionSingleSelect extends StatelessWidget {
  const QuestionSingleSelect({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
