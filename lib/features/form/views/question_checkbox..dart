import 'package:flutter/material.dart';

class QuestionCheckBox extends StatelessWidget {
  const QuestionCheckBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
