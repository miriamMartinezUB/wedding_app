import 'package:flutter/material.dart';

class QuestionFreeText extends StatelessWidget {
  const QuestionFreeText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
